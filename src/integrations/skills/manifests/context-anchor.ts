import type { SkillManifest } from '../types'

const manifest: SkillManifest = {
  key: 'context-anchor',
  label: 'Context Anchor',
  domain: 'memory',
  packageName: 'context-anchor',
  purpose: 'Session orientation and recovery layer for Memory and Command.',
  summaryLines: [
    'Summarizes active context, recent decisions, and open loops.',
  ],
  requires: {},
}

export default manifest
